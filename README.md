This is my first React project. It creates a ToDo List.

Clone the remote repository: https://github.com/la-ry-sa/my-react

To install all the dependencies, run `nmp install`

To run the server, run `npm run build`
